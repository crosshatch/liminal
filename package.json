{
  "name": "liminal-root",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "changeset": "changeset",
    "check": "tsc -b",
    "clean": "node ./konfik/main.ts clean --ignore '**/repos'",
    "dev": "alchemy dev",
    "bootstrap": "alchemy deploy bootstrap.ts --stage prod --yes --profile liminal-env",
    "sync-repos": "node ./konfik/main.ts sync-repos",
    "destroy": "alchemy destroy",
    "plan": "alchemy plan",
    "prepare": "ts-patch install -s",
    "test": "vitest run",
    "watch": "tsc -b -w"
  },
  "devDependencies": {
    "@changesets/cli": "catalog:",
    "@effect/language-service": "catalog:",
    "@effect/platform-node": "catalog:",
    "@effect/vitest": "catalog:",
    "@types/node": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "@vitest/browser": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "@vitest/expect": "catalog:",
    "@vitest/web-worker": "catalog:",
    "alchemy": "catalog:",
    "effect": "catalog:",
    "happy-dom": "catalog:",
    "liminal-util": "workspace:*",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "playwright": "catalog:",
    "ts-patch": "catalog:",
    "turbo": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "packageManager": "pnpm@11.5.0"
}
